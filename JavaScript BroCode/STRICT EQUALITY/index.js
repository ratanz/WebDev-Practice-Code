// = assignment operator
// == comparison operator (compare if values are equal)
// === strict equality operator (compare both value and type)
// != not equal to operator
// !== strict inequality operator (compare both value and type)

const PI = 3.14;

if(PI == "3.14"){
    console.log("That is Pi");
} else {
    confirm.log("That is NOT Pi");
}

// - - - - - - - - - - - - - -

const PI = 3.14;

if(PI != "3.14"){
    console.log("That is Pi");
} else {
    confirm.log("That is NOT Pi");
}