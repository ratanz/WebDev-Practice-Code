<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-black text-white">

        <div class="heading flex justify-center items-center">
            <h1 class="text-3xl font-bold mt-4 text-zinc-200 flex justify-center items-center">Task manager</h1>
        </div>

        <div class="form p-10 text-white">
            <form action="create" method="post">
                <input class="block w-4/12 rounded-md outline-none px-5 py-2 bg-zinc-800" type="text"
                    placeholder="Enter title" name="title">
                <textarea class="block w-full rounded-md outline-none mt-3 px-5 py-2 bg-zinc-800 resize-none text-white"
                    placeholder="Write your task" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md cursor-pointer" type="submit" value="Create Task">
            </form>

            <div class="tasks flex gap-4 flex-wrap mt-4">
                <% if(files.length> 0){ %>
                    <% files.forEach(function(val){ %>
                        <div class="task min-w-72 px-3 py-4 mt-3 bg-zinc-800 rounded-md">
                            <h1 class="text-white text-3xl tracking-tighter">
                                <%= val %>
                            </h1>
                            <div class="flex w-full justify-between items-center mt-3">
                                <a class="text-blue-600 inline-block " href="/file/<%= val %>">Read More</a>
                                <a class="text-zinc-500" href="/edit/<%= val %>">Edit</a>
                            </div>
                        </div>
                        <% }) %>
                            <% } else{ %>
                                <h3 class="text-zinc-600">No tasks yet.</h3>
                                <% } %>

            </div>
        </div>
    </div>
</body>

</html>