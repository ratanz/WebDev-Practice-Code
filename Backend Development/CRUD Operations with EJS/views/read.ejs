<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10">
        <a class="text-blue-500 mb-10" href="/">Back to Home</a>
        <h1 class="text-3xl tracking-tighter mb-3 mt-12 text-white">All Users</h1>
        <div class="users flex gap-4 flex-wrap">
            <% if(users.length> 0){ %>
                <% users.forEach(function(user){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 rounded-lg overflow-hidden">
                            <img class=" w-full h-full object-cover object-center " src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%=user.name %>
                        </h3>
                        <h5 class="text-zinc-500">
                            <%=user.email %>
                        </h5>

                        <div class="flex gap-3 mt-10 justify-between">
                            <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit user</a>
                            <a class="text-red-500" href="/delete/<%= user._id %>">Delete user</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h1 class="text-zinc-500">No users yet.</h1>
                            <% } %>

        </div>
    </div>
</body>

</html>